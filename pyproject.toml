[project]
name = "ysu-net-login"
version = "0.1.0"
description = "燕山大学校园网登录脚本"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.28",
    "urllib3>=1.26",
    "pycryptodome>=3.18",
]

[project.optional-dependencies]
browser = ["playwright>=1.40"]

[tool.uv]
package = false
